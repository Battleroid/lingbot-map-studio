"""Gaussian-splat training processor.

Phase 2 ships a stub so the `worker-gs` container wiring can be exercised
without the real gsplat CUDA wheels. Phase 5 replaces `stub.py` with the
real trainer.
"""
